repos:
-   repo: https://github.com/ravishankarigd/DevOpsBeast/precommit
    rev: v0.2.0
    hooks:
    -   id: terrascan
        pass_filenames: false
        args: [-l=s3] #NOTE: this command should point to the directory containing your .tf files
        verbose: true
